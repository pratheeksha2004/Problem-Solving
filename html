<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Invoicing Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Lato&family=Playfair+Display&family=Poppins&family=Roboto&display=swap" rel="stylesheet" />
    <style>
      :root {
        --font-sans: 'Lato', 'Roboto', sans-serif;
        --font-heading: 'Playfair Display', 'Poppins', serif;
      }

      body {
        font-family: var(--font-sans);
        background-color: #FAF9F5;
        color: #3A6F48;
      }

      .btn {
        @apply py-2 px-4 rounded-lg shadow-md transition duration-200;
      }

      .btn-primary {
        background-color: #3A6F48;
        color: white;
      }

      .btn-primary:hover {
        background-color: #2E583A;
      }

      .btn-accent {
        background-color: #F4C842;
        color: black;
      }

      .btn-accent:hover {
        background-color: #d9b735;
      }

      .card {
        @apply bg-white p-6 rounded-xl shadow;
        border: 1px solid #E5E7EB;
      }

      .heading {
        font-family: var(--font-heading);
      }
    </style>
  </head>
  <body class="min-h-screen flex">
    <!-- Sidebar -->
    <aside class="w-60 bg-[#3A6F48] text-white p-6">
      <h1 class="text-2xl font-bold mb-6">WellInvoice</h1>
      <nav class="space-y-4">
        <a href="#" class="block text-white hover:underline">Dashboard</a>
        <a href="#" class="block text-white hover:underline">Invoices</a>
        <a href="#" class="block text-white hover:underline">Clients</a>
        <a href="#" class="block text-white hover:underline">Settings</a>
      </nav>
    </aside>

    <!-- Main Panel -->
    <main class="flex-1 p-8 bg-[#FAF9F5]">
      <!-- Header -->
      <div class="flex justify-between items-center mb-8">
        <h2 class="text-3xl heading">Invoices</h2>
        <button class="btn btn-primary">+ Generate Invoice</button>
      </div>

      <!-- Invoice List -->
      <div class="grid gap-6 md:grid-cols-2 lg:grid-cols-3">
        <!-- Invoice Card -->
        <div class="card">
          <h3 class="text-xl font-semibold heading mb-2">Invoice #INV-0001</h3>
          <p class="text-[#8B5A2B]">Client: Arogya Wellness</p>
          <p class="text-sm text-gray-600 mb-4">Due: 28 May 2025</p>
          <div class="flex gap-2">
            <button class="btn btn-accent">Send Reminder</button>
            <button class="btn btn-primary">Pay Now</button>
          </div>
        </div>

        <div class="card">
          <h3 class="text-xl font-semibold heading mb-2">Invoice #INV-0002</h3>
          <p class="text-[#8B5A2B]">Client: Veda Naturals</p>
          <p class="text-sm text-gray-600 mb-4">Due: 30 May 2025</p>
          <div class="flex gap-2">
            <button class="btn btn-accent">Send Reminder</button>
            <button class="btn btn-primary">Pay Now</button>
          </div>
        </div>

        <!-- Add more invoice cards here -->
      </div>
    </main>
  </body>
</html>
